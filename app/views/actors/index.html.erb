<%# <% content_for(:title, t('.title')) %>
<div class="container mb-5 pt-5">
  <h1>Actors</h1>
  <%# <div class="row">
    <div class="col-md-12 mb-3">
      <%= render 'search_form' 
    </div>
  </div> %>
  <div class="row pt-3">
    <div class="col-md-12">
      <table class="table table-striped t-5">
        <thead>
        <tr>
          <th scope="col">Name</th>
          <th scope="col">Scenes</th>
          <%# <th scope="col">各actorのシーン保持数</th> %>
        </tr>
        </thead>
        <tbody>
          <tr>
            <td>
              <% @actors.each do |actor| %>
                <%= link_to actor.name, actor_path(actor) %>
              <% end %>
            </td>
            <%# <td>
              34
            </td> %>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
  <%# <div class="row">
    <div class="col-md-12">
      <!-- ページネーション -->
      <%= paginate @users >
    </div>
  </div> %>
</div>
